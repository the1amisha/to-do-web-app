{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A to-do list web application built with HTML, CSS, and JavaScript.",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.1"
  }
}